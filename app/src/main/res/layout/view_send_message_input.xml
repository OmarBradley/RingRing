<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorWhiteBackground"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/image_send_message_input_border_line"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp"
        android:src="@drawable/message_input_border_line_image" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/edit_chat_message"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:hint="여기에 메시지 입력"
            android:paddingTop="8dp"
            android:textColor="@color/colorTextColor"
            android:textColorHint="@color/colorChattingInputText"
            android:textSize="17sp" />

        <Button
            android:id="@+id/btn_sender"
            style="?android:attr/borderlessButtonStyle"
            android:layout_width="45dp"
            android:layout_height="45dp"
            android:layout_gravity="bottom"
            android:background="@drawable/ripple_button_send" />

    </LinearLayout>

</LinearLayout>